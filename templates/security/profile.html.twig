{% extends 'articles/base.html.twig' %}

{% block title %}
    Профиль
{% endblock %}

{% block content %}

    <div class="row">

        <div class="col-xl-6">

            {% for flashMessage in app.flashes('template_message') %}
                <div class="alert alert-success" role="alert">{{ flashMessage }}</div>
            {% endfor %}

            {# <div class="card my-4">
                <div class="card-body">

                    <p>Ваш API токен: asft21sfsvzxvghk1912t1g12m</p>
                    <button class="btn btn-outline-primary">Создать новый</button>
                </div>
            </div> #}

            {{ form_errors(profileForm) }}

            {{ form_start(profileForm) }}

            {{ form_row(profileForm.name, {'label': 'Ваше имя'}) }}

            {{ form_row(profileForm.email, {'label': 'Ваше e-mail'}) }}

            {{ form_row(profileForm.plainPassword) }}

            {{ form_row(profileForm.confirmPassword) }}

            <button class="btn btn-lg btn-primary btn-block text-uppercase" type="submit">Изменить</button>

            {{ form_end(profileForm) }}
        </div>
    </div>

{% endblock %}